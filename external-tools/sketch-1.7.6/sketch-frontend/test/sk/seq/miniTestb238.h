#ifndef MINITESTB238_H
#define MINITESTB238_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK12(int N, float* in/* len = N */, float* _out/* len = N */);
extern void test12(int N, float* in/* len = N */, float* _out/* len = N */);
}

#endif

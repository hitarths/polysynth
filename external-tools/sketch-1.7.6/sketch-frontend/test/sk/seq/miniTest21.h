#ifndef MINITEST21_H
#define MINITEST21_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void reverseSketch(bool* in/* len = 3 */, bool* in2/* len = 3 */, bool& _out);
extern void reverse(bool* in/* len = 3 */, bool* in2/* len = 3 */, bool& _out);
}

#endif

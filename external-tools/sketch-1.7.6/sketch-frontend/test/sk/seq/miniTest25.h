#ifndef MINITEST25_H
#define MINITEST25_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void miniTestSK(bool* x_0/* len = 3 */, bool* _out/* len = 3 */);
extern void miniTest(bool* in_0/* len = 3 */, bool* _out/* len = 3 */);
}

#endif

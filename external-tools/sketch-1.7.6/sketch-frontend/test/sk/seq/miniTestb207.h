#ifndef MINITESTB207_H
#define MINITESTB207_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void sketch(bool* in/* len = 2 */, bool* _out/* len = 4 */);
extern void spec(bool* in/* len = 2 */, bool* _out/* len = 4 */);
extern void tutu(bool* in/* len = 2 */, int n, bool* _out/* len = 4 */);
extern void rvbit(bool* in/* len = 2 */, bool* _out/* len = 4 */);
}

#endif

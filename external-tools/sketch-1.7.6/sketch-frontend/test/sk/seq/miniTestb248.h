#ifndef MINITESTB248_H
#define MINITESTB248_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void rec6ptFragSK(int N, float* in/* len = N */, float* out/* len = N */, int il, int ih, float* _out/* len = N */);
extern void rec6ptFrag(int N, float* in/* len = N */, float* out/* len = N */, int il, int ih, float* _out/* len = N */);
}

#endif

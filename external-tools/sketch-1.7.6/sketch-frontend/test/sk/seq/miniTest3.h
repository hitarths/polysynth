#ifndef MINITEST3_H
#define MINITEST3_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void simple(bool* in/* len = 12 */, bool* _out/* len = 4 */);
extern void simpler(bool* in/* len = 12 */, bool* _out/* len = 4 */);
}

#endif

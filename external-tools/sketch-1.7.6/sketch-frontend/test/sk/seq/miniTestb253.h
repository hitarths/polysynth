#ifndef MINITESTB253_H
#define MINITESTB253_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK0(int N, int* in_0/* len = N */, int* _out/* len = N */);
extern void test0(int N, int* in_0/* len = N */, int* _out/* len = N */);
}

#endif

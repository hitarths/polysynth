#ifndef MINITESTB241_H
#define MINITESTB241_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK15(int N, int* in/* len = N + 3 */, int* _out/* len = N */);
extern void test15(int N, int* in/* len = N + 3 */, int* _out/* len = N */);
}

#endif

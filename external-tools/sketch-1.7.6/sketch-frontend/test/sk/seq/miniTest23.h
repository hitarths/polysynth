#ifndef MINITEST23_H
#define MINITEST23_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void miniTestSK(bool* x/* len = 3 */, bool* _out/* len = 3 */);
extern void miniTest(bool* in/* len = 3 */, bool* _out/* len = 3 */);
}

#endif

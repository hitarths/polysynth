#ifndef MINITEST42_H
#define MINITEST42_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void mergeSort(bool* input_0/* len = 3 */, bool* _out/* len = 3 */);
extern void sort(bool* input_0/* len = 3 */, bool* _out/* len = 3 */);
}

#endif

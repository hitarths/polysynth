#ifndef MINITEST90_H
#define MINITEST90_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void first_sk(bool* x/* len = 4 */, bool* y/* len = 4 */, bool* _out/* len = 4 */);
extern void first(bool* x/* len = 4 */, bool* y/* len = 4 */, bool* _out/* len = 4 */);
}

#endif

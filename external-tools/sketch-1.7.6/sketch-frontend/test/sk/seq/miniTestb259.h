#ifndef MINITESTB259_H
#define MINITESTB259_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK5(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test5(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif

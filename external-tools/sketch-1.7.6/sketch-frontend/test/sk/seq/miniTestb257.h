#ifndef MINITESTB257_H
#define MINITESTB257_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK3(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test3(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif

#ifndef MINITESTB258_H
#define MINITESTB258_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK4(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test4(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif

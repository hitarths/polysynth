#ifndef MINITESTB246_H
#define MINITESTB246_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK16(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test16(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif

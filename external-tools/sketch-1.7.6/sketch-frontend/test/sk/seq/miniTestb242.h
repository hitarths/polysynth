#ifndef MINITESTB242_H
#define MINITESTB242_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK1(int N, float* in/* len = N */, float* _out/* len = N */);
extern void test1(int N, float* in/* len = N */, float* _out/* len = N */);
}

#endif

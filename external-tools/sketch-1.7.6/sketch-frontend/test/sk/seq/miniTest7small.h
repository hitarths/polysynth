#ifndef MINITEST7SMALL_H
#define MINITEST7SMALL_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void reverseSketch(bool* in/* len = 2 */, bool* _out/* len = 2 */);
extern void reverse(bool* in/* len = 2 */, bool* _out/* len = 2 */);
}

#endif

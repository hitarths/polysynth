#ifndef MINITESTB274_H
#define MINITESTB274_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void sk(int N, int M, bool* in/* len = N * M */, bool* _out/* len = N * M */);
extern void test(int N, int M, bool* in/* len = N * M */, bool* _out/* len = N * M */);
}

#endif

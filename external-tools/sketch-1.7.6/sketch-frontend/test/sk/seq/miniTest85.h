#ifndef MINITEST85_H
#define MINITEST85_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testsk(bool* in/* len = 3 */, bool* _out/* len = 3 */);
extern void test(bool* in/* len = 3 */, bool* _out/* len = 3 */);
}

#endif

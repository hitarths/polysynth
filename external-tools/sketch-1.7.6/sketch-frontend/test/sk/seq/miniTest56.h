#ifndef MINITEST56_H
#define MINITEST56_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void normalRoundsk(bool* in/* len = 32 */, bool* _out/* len = 32 */);
extern void normalRound(bool* in/* len = 32 */, bool* _out/* len = 32 */);
}

#endif

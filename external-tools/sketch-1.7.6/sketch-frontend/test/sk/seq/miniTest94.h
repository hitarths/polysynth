#ifndef MINITEST94_H
#define MINITEST94_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void reverseSketch(bool* in1/* len = 5 */, bool* in2/* len = 5 */, bool* _out/* len = 5 */);
extern void reverse(bool* in1/* len = 5 */, bool* in2/* len = 5 */, bool* _out/* len = 5 */);
}

#endif

#ifndef MINITEST95_H
#define MINITEST95_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void sklog2(bool* in_0/* len = 4 */, bool* _out/* len = 4 */);
extern void slog2(bool* in/* len = 4 */, bool* _out/* len = 4 */);
}

#endif
